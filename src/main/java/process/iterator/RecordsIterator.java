package process.iterator;

import entity.CreditCard;

import java.util.Iterator;

public interface RecordsIterator extends Iterator<CreditCard> {
    //Add additional methods for iteration control if needed
}